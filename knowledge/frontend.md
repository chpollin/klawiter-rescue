---
title: Interface and Curation
aliases: [frontend, interface, EIL, curation tool]
project:
  name: Klawiter Bibliography
  repository: https://github.com/chpollin/klawiter-rescue
status: maintained
language: en
version: 1.7
tags: [frontend, eil, accessibility, export]
created: 2026-03-29
updated: 2026-09-23
authors: [Christopher Pollin]
related: [data, pipeline, testing, production-readiness]
---

# Interface and Curation

## Purpose

The static application under `docs/` makes the rescued holding searchable and provides a local Expert-in-the-Loop curation surface. It needs no build step and no server process beyond a static localhost server for edit mode.

The public application supports search, facets, a linked exploration dashboard, map and reference views, detail cards, a data-quality workbench and citation exports. The local curation layer adds source evidence, provenance, triage, subject-level authority decisions and a versioned patch export. The interface language is English throughout, edit mode included.

## Architecture

`docs/index.html` loads classic scripts (no module system, one shared global namespace) and locally vendored dependencies (`docs/vendor/`: FlexSearch, D3 v7, topojson-client, d3-sankey, `countries-110m.json` with a provenance note). FlexSearch is loaded with `defer` because every visit needs the search index. The three visualization bundles are fetched by `App.ensureExploreLibs` on the first switch to `#stats`, in a fixed order and behind a single promise gate; if that fetch fails, the Explore view shows an error with a retry while search and the result list keep working. The application contacts no external host at runtime.

| Module | Task |
|---|---|
| `app.js` | data loading, state, hash routing, result list, cards, edit-mode switch |
| `constants.js` | type and period labels, colour constants, chart dimensions |
| `utils.js` | escaping, highlighting, counting and download helpers |
| `home.js` | start view with figures and entry points; one plain row per category |
| `facets.js` | facet sidebar of the result view |
| `detail.js` | entry detail in two layouts, named field blocks for reading and, in edit mode, the same blocks read-only above the page record with its editing and adjudication controls |
| `edit.js` | local field and reconciliation curation, triage hints, source evidence, patch export |
| `curate.js` | data-quality workbench (`#data/quality`): completeness matrix, work queues, candidate queue |
| `export.js` | BibTeX, RIS, single-entry and full export, permalink |
| `pages.js` | the two static pages About and Data, figures taken from `_meta` |
| `jsonld-playground.js` | interactive JSON-LD view with escaped syntax highlighting, rendered as a section of the Data page; addressable through `#data/playground/<pageId>`, suggestion list as a `listbox` with arrow and Enter operation and its own empty state, view buttons carrying `aria-pressed`, document listener registered once, entry types read from `constants.js` |
| `explore.js` | Explore frame: modes, the shared filter set, URL state, detail panel |
| `explore-timeline.js` | linked dashboard: coverage, neutral decade histogram, ranked language/type filters and entry preview |
| `explore-geography.js` | globe and map view from the vendored geometry |
| `explore-network.js` | reference ranking (most-referenced entries) and translator Sankey |

The application loads `docs/data/klawiter.json` blocking and checks `resp.ok`. If that load fails, a full-page error state replaces the start view, names the cause and offers a retry; navigation and the search field are visibly disabled meanwhile, because without the holding neither resolves anything. The publication layer of a page is fetched from `docs/data/publications/<sourcePageId>.json` when the record does not carry it inline, once per page and session, and the open card is re-rendered when it arrives. `reconciliation.json` is loaded where its data first becomes visible (expanded card, edit mode, workbench) through a shared load promise, and a card that is already open is refreshed once the data arrives. `triage.json` is fetched only on entering edit mode. The search index is built after first paint through `requestIdleCallback`, with a `setTimeout` fallback and a lazy build on the first search as the last resort. Data verification (`verifyData`) runs on `localhost` only, because it is a development instrument and costs several full passes over the holding. Missing reconciliation data produces no invented links; the affected areas stay empty.

## Navigation, Routes and Page Layout

The header navigation carries four destinations, Overview, Explore, Data and About, joined on localhost by the Edit switch as its last item in the same form as the links. The search field sits on the burgundy bar as a lighter surface without a frame, so the underline under the active navigation item is the only line in the header. There is no dropdown and no research-network bar above the header; what those held has moved into the two text pages or into the footer. The footer carries two blocks, credits on the left with compiler, publishing context, the Promptotyping attribution, the producer credit and the licence line, and the link column on the right, which runs in two columns over About, Data, Data Quality, Stefan Zweig Digital, Ontology & Data and GitHub. The imprint is reachable from the credits text and carries no link of its own, because it is a section of About.

| Route | Content |
|---|---|
| `#` | start page with search, browse entry point and the category groups as one flat list |
| `#stats`, `#stats/<mode>` | Explore dashboard overview, map and connections view; `timeline` remains the compatible overview route |
| `#data`, `#data/<section>` | data model, specification and vocabulary, downloads, JSON-LD playground, pointer to the workbench |
| `#data/playground/<pageId>` | playground with a preloaded entry; the action bar of an entry card links there as "Playground" |
| `#data/quality` | data-quality workbench, a sub-page of Data |
| `#about`, `#about/<section>` | project, methodology, help and imprint as sections of one page with anchor navigation |
| `#browse`, `#q=…`, `#entry=…`, `#entry=…&pub=…`, facet parameters | result list; `pub` opens the card at one publication, the address its citation carries |

User-triggered state changes (choosing a facet, removing a chip, submitting a search, changing the sort) write a real history entry; `replaceState` is reserved for normalizations that only catch the URL up with a state it already carries. The back path therefore leads through the research steps instead of leaving the application after the first interaction. The router ignores the fragment `#main-content` of the skip link, because it addresses an element rather than a route.

An `#entry=` permalink names itself in the result line as "Permalink — 1 entry", written by the line itself rather than after the render, so switching edit mode or the sort keeps the label. That route carries one record, so the sort control, the facet sidebar and the mobile filter button stay hidden on it while the export stays. If the identifier does not resolve, a state of its own appears that points at a possible redirect and offers a way to the start page; a `#title=` without a hit in the redirect map and the title index leads to the same state instead of silently falling through to the start page. The static pages and the workbench reset query, filters and the search field the way the Explore branch does.

A static page addresses its sections through a suffix in the hash (`#about/imprint`); the router renders the page and then scrolls to `sec-<section>`. The published deep links of the earlier page layout stay valid and are rewritten in the router to their section, `#methodology` and `#help` and `#imprint` to `#about/…`, `#jsonld` to `#data/playground`, and `#quality` to `#data/quality`. The workbench hash takes the same rewrite path, so it never reaches the branch that would render it under its former address. The document title is set in `App._updateTitle` alone, with the base title "Klawiter — Stefan Zweig Bibliography" as shipped in the HTML.

The About page takes the figures of the holding dynamically from `_meta` (holding size, languages, year span, field coverage, number of types), so prose and data state cannot drift apart. The Data page names the files shipped under `docs/data/` with their role and marks the canonical graphs under `data/output/` as reachable through the repository only. The full export is called "Download dataset (JSON)", because it delivers the flat projection in frontend key names and carries no `@context`. It stands on the Data page alone, as a text link with its icon, rather than at the foot of Explore as well.

## Design and Accessibility Contract

The interface uses the established Stefan Zweig Digital palette, Source Serif 4 for reading text and Source Sans 3 for navigation and controls. CSS variables are the only source for colours and spacing. Libraries and fonts are local.

The visual language is deliberately narrow. Letterspaced capitals mark the title of a view and nothing below it, so section headings are plain serif in the dark gold token or in burgundy. Reading pages, the category groups of the start view and the panels of Explore sit directly on the cream ground, where a small heading and the whitespace above it separate one block from the next, and the anchor navigation of a page carries no rule under it. Tables structure themselves through a hairline between rows under a single header rule, without an outer frame. The entry type and the review status read as plain subdued text in both card layouts, because colour on a card is reserved for a state a reader has to act on, meaning a gap in the completeness matrix, a contested claim and the triage hint of edit mode. There are two button styles, filled burgundy for the action a view exists for and outlined for every other action; compact controls inside lists and rows (facet items, legend entries, the per-field controls of edit mode) are not buttons in this sense and keep their in-place form, and inside an entry card an action is borderless text with a leading icon, because the card already has its one outline. A control that only navigates or downloads is a text link with its icon rather than a button, and a set of views over one surface (the Explore modes, the playground views) is a row of text tabs whose active member carries the underline of the main navigation.

Semantic HTML, visible keyboard focus, labelled forms, ARIA labels for iconic controls and sufficient contrast form the basis. Gold that carries text uses the darkened token that clears 4.5:1 against white and cream. After a view change `showView` moves focus to the heading of the view or to `#main-content`, so hash navigation reaches screen readers; a change that follows from typing in the search field leaves the focus there. That focus is a starting point rather than a keyboard position and draws no ring, while links, buttons and fields keep theirs. All keyboard shortcuts bail out when Control, Meta or Alt is held. `prefers-reduced-motion` is respected globally (animations and smooth scrolling are dropped). A state is never conveyed by colour alone. Mobile layouts preserve search, filters and detail information without horizontal scrolling; the completeness matrix scrolls in its own container.

## Research Views

- The overview shows extent, temporal distribution, languages, types and places. A category is one row that opens the filtered list; the former expandable subcategories assumed a "format and language" structure most categories do not have. The groups form one flat block that balances over three columns on a wide screen, two on a tablet and one on a phone, with the count right-aligned under its group heading. Three columns keep the block no taller than its longest group, where two left the short groups stacked under the long one. Browsing is a text link beside the search field, because the field opens the same view.
- The result line is the heading of the view and says what was asked for, a count with the query, a count with the filters that produced it, or the size of the whole holding where neither applies; sort and export stand beside it as text controls. On the permalink route the line reads as the permalink label instead.
- The result list combines full-text search, facets and sorting. A result in the list is a plain item, the title as the line it is opened by and under it one subdued line of values separated by middle dots, with vertical space alone between the items. The frame, the white surface and the inner padding belong to the opened card, which the permalink route is; a result in the list carries no box, no colour bar and no type chip. The card head is the title with one line under it, which carries type, year, language, place, publisher and page count in a list, because those values are the readable form of the filter axes; the source excerpt is dropped, because the expanded card carries the full entry. A page documenting several publications has no single one of those values, so its head counts its publications instead and leaves the values to the blocks below. On the permalink route `#entry=` that line keeps the type, adds the kind of the page with that count, and marks the title of an author page as the name of an author; every value stands named in the blocks below. The review status closes the line in both layouts, so the state of the record is read where the record is named, and it is plain subdued text like the other items of the line rather than a filled chip. The items sit in one flow whose separator is drawn on the item that follows, so a line wrapping on a phone never ends on a dangling middle dot. The card header carries `aria-expanded` and `aria-controls` pointing at the detail id.
- The search index folds diacritics (`charset: 'latin:advanced'`), so the transliterations of the holding are findable in plain spelling as well. A search runs from two characters on. The hit count is capped at 5,000, and the result label says so once the cap is reached. The header field and the start-page field share one handler, the start-page field is prefilled with the current query on render, and focus moves into the header field on the switch to the result view. Highlighting marks the raw text and escapes segment by segment, so an apostrophe, ampersand or quotation mark in the query matches and no entity is torn apart.
- The year, decade, period, language and location axes read the publication layer. A page belongs to every year, language and place of its publications, so a page whose second edition is from 1960 answers a filter on that year and a page carrying a German book beside an Arabic article stands under both languages. The union arrays of the record are the source for those axes and the flat field answers where they are absent. The filter predicate, the facet counts and the Explore aggregations read the same rule, so a count and the list it opens hold the same entries. A page is counted once per value it carries, which is why the counts of an axis add up to more than the size of the holding.
- Facet counters follow the standard drilldown, so every facet counts against the set that all other filters select and the alternatives of a chosen facet stay selectable. Language and place show the most frequent values and expand per group to the full holding. Type, language and review stand open, period and location fold away behind their heading, which acts as the toggle and says how many of its values are active while it is closed. Chips carry labelled closers and, from two active filters on, a "Clear all" chip; the empty state offers the same action.
- The review facet reads the review state of a record, the statuses the dataset carries and the absence of a decision as a value of its own, with a further value for records holding open review flags. Its values and their words are derived from the holding rather than declared, and the axis is addressable like every other filter (`#review=unreviewed`).
- An opened card carries Previous and Next in its head row. In the list they close it and open the neighbour in the current order; on the permalink route they appear only while the session still holds a rendered list and then lead to the neighbour's permalink. At the ends of the list they are absent.
- On the result view slash reaches the search field, j/k and the arrow keys walk the card headers, Enter or Space opens the focused card and Escape closes the open one. Escape inside the search field leaves the field and keeps the query and the list, because the native clear of a search input would otherwise commit an empty query and return to the start view. The list itself is one tab stop with a roving tabindex, the way the workbench queue has it. Tab enters at the header the walk is on, the arrows and j/k move inside the list, and the next Tab leaves it instead of stepping through fifty headers. In edit mode j/k keep stepping from expanded card to expanded card, which is the adjudication walk the triage sort exists for.
- The mobile drawer moves the sidebar into the overlay by DOM move and back on close instead of cloning its markup, so it shows the same groups in the same open state. Focus jumps to the close button on open, stays cyclic inside the panel, Escape closes, a selection closes, and focus returns to the opener. The mobile filter button appears on the result view only.
- The card carries one thin outline and nothing framed inside it. A field is a flat row of label and value, sharing one label column from tablet width up and wrapping under the label on a phone only where the value is long; inner structure comes from whitespace, small headings and a hairline between publications. Nothing inside a card is an outlined button, so the exports of a publication are two text links in its heading row and the actions at the foot of the card are borderless text with a leading icon.
- Where the record carries the publication layer, the reading view renders one block per publication in source order. A block is headed by the year and by what distinguishes that publication, its edition statement or its own title, and carries its fields under their own names, title, language, place of publication, publisher, extent in the notation of the source, credits with their role, contents with their pages, the container of an article, an online address, series and note. A contribution keeps the credits written under it, so the translator of the verses of one contribution stays with that contribution rather than with the publication. A credit stands under the role of the vocabulary, and where the phrase of the source says more than the plain "<Role> by", what it adds stands visibly after the name in subdued text, so a phone distinguishes the translator of the verses from the translator of the prose without a tooltip. What the extraction rules leave open on a publication stands at the end of its block as one tinted review line in the words of the record. Name variants of a credited name form rows of their own with the source line each stands in, and no identity between the spellings is asserted. Categories, cross-references, reprints and translations belong to the source page and stand as rows above the publications, because they hold for every one of them.
- A tooltip carries what a line does not already state, an identifier, a code or a phrase of the source, and it is written once: `Detail.HELP` maps a label to one sentence and answers for every field label, for the review line of a publication, for the page kind in the head and for the review status. A phone shows no tooltip, so the language subtag, the flag code, the claim identifier, the full checksum and the full wording of a credit phrase are the only things that live there alone. In edit mode the label of an editable row carries the scope of the edit as well.
- The publication layer ships inline in the record or as a per-page file under `data/publications/`. The card reads whichever the dataset provides and fetches the file once per page and session, with the load visible while it runs. A failed request is reported with its cause and the card falls back to the flat fields, so a card is never left empty by it.
- A page without that layer keeps the flat projection. Its reading view is one field block in which every bibliographic field of the record stands under its own name, original title, year of publication, language with its registered subtag, place of publication with the Wikidata authority record as an addition to the field, publisher, extent, translator, contents, reprints, translations, cross-references and categories. Contents keeps the structured list with its page references. Each field shows the provenance class the record holds for it, and the review decision where the review object carries one for that field. The legend of those classes stands once above the result list rather than in every card, on the list route and on the permalink route alike, so a card carries data and not its own key. On the permalink route it stays at the top of the viewport while the card scrolls under it, because a single card runs far past the line that explains its badges. A field the layer calls missing appears with that badge and the state "Not recorded" instead of a placeholder value. The extent quotes the notation of the source wherever a token anchored on the recorded number is derivable, with the numbered extent beside it, and otherwise says in its label that the extent is the numbered one.
- A page without the layer that holds several dated publication blocks, or categories in more than one language, carries a note that every field is a first match and can belong to a different publication.
- On a page with several publications each block carries its own two citation exports as text links in its heading row, because a citation has to say which publication it describes. The bar at the foot of such a card collects every publication of the page and says so.
- The complete Klawiter entry closes the card as the authority the fields were structured from. It stands open on the permalink route and wherever it is all the expansion has, and it stays collapsed in a result list. The identifiers of that source record, page, text and blob, stand with it rather than in a card foot, and an address printed in the source text is a link. Where a publication reports `textStart` and `textEnd`, the offsets into the text the card shows, the block it was read from is marked in that text; the segmentation offsets beside them address the raw source and mark nothing. Contested claims stay visible in the reading view. A field whose value an open claim is about carries the word "contested" beside the value as a link into that claim block, derived from the kind of the claim, so a place claim marks the place rows of the page and of every publication whose imprint carries the contested place. A claim applies to a value only where its subject equals the displayed value, so a claim never marks a page whose places it does not name. Such a claim is a tinted section without a frame whose heading names what is contested and stays open, with the competing interpretations one to a line under their authority identifier rather than their address, the source evidence with its checksum abbreviated to the ends, and the review history in plain words; the claim identifier and the rule that no interpretation is emitted as a confirmed relation are the tooltip of that heading. A category link filters on the category itself (`#category=`); a contents item whose title has an entry of its own becomes an `#entry=` link.
- Redirects do not appear as search hits of their own; the redirect map (including the page-title aliases from stage 05) leads queries and cross-references to the canonical entry.

## Explore

`#stats/timeline` is the dashboard overview. The route name remains compatible with existing links; the visible tabs are Overview, Map and Connections. `#stats` resumes the last mode in the session. The overview replaces the former dense multicolour stacked timeline, chart-mode switches and overlapping milestone annotations.

The dashboard counts **source-page bibliography entries**, not distinct publications, and reads their years, languages and places from the publication layer. A page therefore stands in the decade of each of its editions and under each of its languages, counted once per value, so the decade histogram is labelled in pages and a bar states the number the list behind it holds. A recorded year may still need semantic review. One row of plain figures reports selected entries, entries with/without a recorded year and recorded languages. The missing-year figure opens the affected entries. Missing language stays a visible “Not recorded” group, distinct from any aggregation of recorded languages.

The neutral decade histogram supplies context across the current non-date filters; the selected date range is highlighted. Every populated decade is a labelled keyboard-operable button with its exact count. Explicit From/To fields provide a second selection path. Reversed ranges fail validation. Choosing a decade replaces a year range and vice versa; a date filter excludes entries without a finite positive recorded year. “All years” clears that axis.

Language and entry-type rankings are linked filters with counts and `aria-pressed` state. Each ranking ignores its own selected axis when calculating alternatives, while retaining the other filters, so neighbouring choices remain available. Multiple values within an axis use OR; different axes combine with AND. Selected values and the missing-language group remain reachable beyond the top ranks. “Show all” expands the relevant ranking.

The active-filter bar names the selection and offers reset. The preview lists the first five matching entries in source order, each linked to its record, and “View all” transfers the complete selection to Results. Repeated URL keys preserve multiple languages/types through handover, reload and back navigation. Filtered country and undated custom lists remain session views with a return path; they are not independently shareable persistent filter URLs.

Map and Connections retain the shared filter set and their sidebar controls. The map reports unlocated/unmapped records separately from displayed locations. Connections offers reference rankings and translator flows; its names and edges derive from the flat page data and are not complete edition-level translation relations. Referring links may reproduce a source error even when technically resolved, as documented in [Status](status.md).

Overview uses a full-width panel layout rather than duplicating ranked chart filters in a second sidebar. The panels carry no frame and no fill, each one opens with its heading, and the figures and rankings reflow on narrow screens. The selection line and the mode tabs carry no rules either, so the view is held together by whitespace. What a control does is stated on the control, in its label, its `aria-label` or its `title`, rather than in a standing sentence beside it. The caveat that a counted page may cite several editions stands in this document and on the Data page instead of over the charts. Meaningful controls remain buttons/forms, while the timeline avoids drag-only selection. The [dashboard evidence](evaluations/2026-09-05/dashboard-browser-qa.json) and [testing guide](testing.md) define the paths actually checked; these checks do not establish comprehensive accessibility or usability acceptance.

## Data Quality Workbench (`#data/quality`)

The workbench prioritizes curation from the published artifacts; its counters do not establish that all data is correct:

- A status panel with three figures that direct the work, open authority candidates, contested claims and pending decisions of the current session. Everything the panel used to count as well stands in the matrix and the queues below, where it is also clickable.
- A completeness matrix of field against entry type; every cell with gaps opens the affected entries as a result list, and says so in its own label instead of in a sentence above the table.
- Work queues as clickable lists over census anomalies, values not evidenced in the source text, detectable but unextracted values, LLM-derived fields, unresolvable cross-references (with a list of the most frequent red-link targets) and contested authority claims.
- A subject-level candidate queue for translator and publisher names, sorting open cases by reach (number of occurrences) first. It is publicly readable, and deciding is possible in local edit mode only.

The queue is a `listbox` with a roving tabindex, so it is a single tab stop that lands on the row where the work begins (the subject last worked on, otherwise the first open case). On the keyboard in edit mode, arrows and j/k move, y confirms the top candidate, n rejects, u holds open, z and Backspace take back the last decision, and Enter opens the affected entries. All queue keys bail out when Control, Meta or Alt is held. The way back from the result list finds the subject last worked on by its identifier, scrolls to it and sets focus. Every row shows the top candidate and holds the others in an expandable block with a confirm button per candidate, so a decision is not forced onto the highest score. A redraw of the queue rewrites the status panel above it, because its counters for the running session would otherwise contradict the save counter.

If `triage.json` or `reconciliation.json` fails to load, the module records that in its load state, and the affected counters and blocks mark themselves as "not available (failed to load)". A load failure is thereby distinguishable from a clean holding.

The workbench lists are session views without a hash address of their own, because they derive from artifacts rather than from filter state. The document title then takes the list label, and above the list stands a visible way back to the view it was opened from (workbench or Explore).

## EIL Curation Interface

Edit mode is active on `localhost` only; the switch checks `isLocal` in the setter, and the published site cannot enter the mode even through the console. The switch in the header is additionally view-dependent and appears only where editing actually applies, on the start page, the result list and the workbench; on the text pages and in Explore it is hidden. The mode state itself survives a view change.

The edit layout shows the publications of a page as the reading view does and leaves them read-only, and the editable rows stand below them under the heading "Page record", because the patch contract addresses the source page and carries no target for one publication. Where a page documents several, a status line in the form of a review flag says that the page record holds one value per field for that page and that an edit applies to the page record. Every editable row names the same scope in the tooltip of its label, and a pending action marks the page record alone, so a publication row keeps the provenance class of its own record. Publisher, place of publication, translator and page count are editable. Every field action is typed:

- `accept` confirms an existing value;
- `correct` replaces a value and preserves its predecessor;
- `add` fills a previously empty value.

For every action the interface shows the evidencing passage or the complete source text. Triage hints prioritize round-trip deviations, missing or model-supported provenance and census anomalies; a provenance legend explains the R/L/E badges. In edit mode j/k walk the result list card by card, and the sort "Needs review first" orders by the most urgent data signal.

A field with an open correction shows the corrected value; the replaced dataset value stands struck through beside it, so a re-render does not appear to take back what was just typed. An empty field carries a labelled add button beside the placeholder area that focuses it. In a `contenteditable` field Enter completes the input (line break suppressed, field left) and Escape discards it back to the rendered state; every field carries `role="textbox"` and an `aria-label` with the field name. All controls of a card run through event delegation with data attributes, in the action bar as well as in the candidate blocks; place and agent candidates share one renderer parameterized by the kind of subject.

Running changes stay in `localStorage` until export, together with the timestamp of the last write; the save counter in the header shows the unsecured state and, for a state restored from an earlier sitting, the note "resumed from <date>". If a reload brings back open decisions without edit mode being on, a note beside the counter says the mode has to be switched on to continue. After the patch download the interface asks whether the session is cleared or continues with the open decisions, because the download alone says nothing about what happens to the session.

Session state is separate from the data state. An unsecured field action makes the entry "edited (unsaved)"; "approved" is reserved for the `review` projection of the dataset, which the pipeline writes after an applied patch.

Place reconciliation offers `confirm`, `reject` and `unresolved`; `unresolved` records competing readings as an open claim. Translator and publisher candidates offer the same actions, and since Gate 2 the source occurrences per agent subject are collected from the classified source and published under `sourceOccurrences` in `docs/data/reconciliation.json`, so an unresolved agent decision is evidenced against the source. Both kinds of candidate are subject-level; the interface states the reach ("applies to N entries"). Candidates stay proposals; nothing is published without a decision. The combined export uses `patchVersion: 2` and `reconciliationPatchVersion: 1`.

The review status reads the `review` field of the entry, whose semantics and field-scope limitation are in [[data]]. A place decision alone does not certify the year or translator. An entry without that field stays unchecked, and an unsecured edit of the running session remains a state of the interface.

## Rendering of Contested Statements

Confirmed relations and contested statements are rendered separately. An open authority claim shows the source value, the candidates, the decision history and the open status. The adaptation case on page 4916 is decided since 22 September. Its block stays in the card under the heading "Work identity decided" and shows both readings marked accepted or rejected, the review history with the dated decision, what the decision leaves open for review and the source identifier. The count of contested claims in the workbench counts open claims only, authority and source-revision claims together.

A decided authority claim, such as the compound places "Sofija, Varna" or "Bloemfontein, Kaapstad", appears under "Place assignment decided" on the pages whose place or imprint line carries its subject, with each reading's status, the basis of each review step and the review notes. It sets no value mark and no citation note.

The review label of an entry reads its `scope`, so an entry whose place authority was decided reads "Place authority agent-verified" and a rejected candidate reads "Place authority candidate rejected". The confirmed mark and the Wikidata link stand at the decided place alone. Each publication heading states its edition as proposed, confirmed or contested. A co-imprint reads as one imprint row of publisher and place pairs, and a series gloss stands in brackets after the series.

A page restored from before a Redirect fixer overwrite shows a source-revision note above the Klawiter source naming the published revision, its author and date and the reason, with the later fixer revisions in a collapsed list. A withheld redirect, reached by `#entry=` or `#title=`, shows "Redirect withheld" with its open claim, and a reference to it carries the same note.

The interface does not turn a contested candidate into a clickable confirmed authority link. Figures count publishable relations only. The number of open claims is reported separately.

## Data Page and JSON-LD Playground

The Data page describes the model, the shipped files and the vocabulary without repeating figures that the vocabulary itself publishes. The `klawiter:` namespace resolves to the vocabulary documentation, and the page points there instead of naming a term count that ages with the next vocabulary round. The prefix and namespace table is gone, because the `@context` block in the playground states the same bindings against a live record.

The playground shows one entry in three views, compact, expanded and as triples. In the compact view the `@context` sits in a collapsed disclosure above the record, because it is identical for every entry and long. The record itself shows only keys the `@context` defines. A key without a term definition carries no IRI and would be dropped by a conformant processor, so showing it would suggest a term the vocabulary does not publish; this is what removes `_provenance`, `review` and `locationSameAs` from the view.

## Export

- BibTeX and RIS cite the publication the reader chose. A page with several publications offers the two exports per publication and collects all of them in the export at the foot of the card; a page with one publication cites it as soon as its page file is loaded. While the publication layer is not loaded, the flat compatibility fields answer, as they did before. A publication citation carries its own imprint, places, edition statement, extent, series, container and credits, with translators, including the translators of contributions, and editors in their own fields, the note of the source, the pairs of a co-imprint and a series gloss in the note. BibTeX follows biblatex, writes a series number to `number` and adds `journal` beside `journaltitle`. RIS follows the tag use of Zotero's RIS translator, translators in `A4` and editors in `A3` for books and `A2` for articles, one `CY` per place. The address is every place of that publication in the wording of the source, joined as the imprint has them, because naming one of them alone settles a place assignment the source leaves open. Where an open authority claim concerns the place of the cited publication, the citation says in its note that the place assignment is contested, read from the same reconciliation claims the card shows. Single and batch exports wait for those claims to load. Each publication is cited with its own `#entry=…&pub=…` address. `pagetotal` takes the numbered extent, and the notation of the source goes to the note where the citation carries one anyway.
- BibTeX and RIS of the flat record serve citation transfer of a page without the publication layer. Both read the same type rule and include the permalink (`url` and `UR` respectively); the recorded extent stands in `pagetotal`, because `pages` means the page range inside a container. A run on `localhost` or over `file://` cites the published address, so the link resolves for the recipient.
- The author of a citation follows the role the source gives each party (decided by the main instance after delegation by the operator on 2026-09-22, revisable, replacing the grouping rule of 8 September). Zweig is the author only of the Works group without the forewords and of the collected works. A translation page and a foreword page are indexed under the author of the translated or prefaced text, and an author page under the author of its publications, so these three take the part of the page title before " / " as the author. Zweig keeps his translator or contributor credit there, a person credited twice in one role is named once in the role field, and where no credit of the cited record names Zweig the note says that the volume contains a translation or a foreword or afterword by him. The title part counts as an author only in the form of an inverted personal name, "Surname, Forename", with at most four words on either side of the comma. A list marker, a heading, a book or article title and a shorthand for several people give no author, because a citation without an author misleads less than one with a title in the author field. Pages about Zweig carry no author and name him as a keyword. A publication whose source credits its own author ("A graphic novel by") is cited under that author, whatever page lists it, which is the per-publication exception to the page rule and so far applies to the graphic novel on pages 4916 and 5110. The type sets are derived from the Overview grouping rather than kept as second lists. The JSON-LD playground and the JSON-LD export still follow the canonical flat projection, which names Zweig as author of every type outside the reception group.
- The batch export of the result list labels itself with the hit count, asks back above a thousand entries, and writes one citation per publication. It loads the publication file of every exported page that has one, a few at a time and with the button saying that it is working, and falls back to the flat citation for a page whose file does not exist or does not load. Afterwards the button states the number of citations, which is not the number of exported pages.
- Copying the permalink catches a denied clipboard and then offers the address in a selectable field.
- The single-entry JSON-LD export adds existing edition claims as graph nodes of their own, under the properties `klawiter:hasContestedClaim` and `klawiter:hasReviewAction` of the current vocabulary; the class names `ContestedClaim` and `ReviewAction` are unchanged.
- The full export carries contested edition and authority claims alongside the confirmed relations.
- The canonical edition and reconciliation graphs remain under `data/output/editions/` and `data/output/reconciliation/`. They have broader structure than a browser record export but do not yet carry every edition-specific bibliographic field.

A claim with the predicate `schema:exampleOfWork` is exported without setting the corresponding confirmed relation on the edition node at the same time.

## Validation

Node tests cover source-evidence logic, triage order, reconciliation lookup, a stable patch export, the separation of contested claims, the routing guard including the edit-mode gate, the redirects of the published deep links, the section suffix of static pages, the visibility rule of the edit switch and the order of the candidate queue. `tests/entry_card.test.js` holds the card contract, meaning the single-entry state the `#entry=` route sets, the head that keeps type and title there, the named field rows with their provenance and review marks, the missing field without a placeholder, the quoted extent notation, the source identifiers at the source block, the linked address in the source text and the note on a page with several publications. It also holds the publication layer, the marked source block of a publication and its absence without text offsets, the citation buttons per publication, the block per publication with its own extent, the credits of a contribution against the credits of the publication, the review hint, the name-variant block, the page-kind label with its count in both card heads, the credit phrase that says more than its role, the contested mark that answers to the address of its claim block, and both load paths of the per-page file, the fetch with its visible load state and the failure that falls back to the flat fields. The workbench route is asserted in both directions, as a redirect of `#quality` and as the one page route on which the edit switch appears. `tests/edit_session.test.js` holds the session state, meaning the rendered correction value with its struck-through predecessor, the add control on an empty field, "edited" instead of "approved", the index over contested claims, the category link, the review-status chip and the opened source in the reading view, and the playground route. It also holds the scope of the edit layout, the read-only publication blocks against the editable page record and the status line that names the scope on a page with several publications. `tests/citation_export.test.js` holds what a citation describes, the publication with its own imprint, extent, credits and container, the two editions of one page apart, the collected export of a page and the fall back to the flat fields while the page file is not loaded. It also holds the author rule by source role with the name-form check and the per-publication exception, the full place wording with the contested note, the numbered `pagetotal` with the source notation in the note, and the batch export that writes one citation per publication and reports how many. Added to that are the search guarantees in `tests/search_logic.test.js` (diacritic folding of the index, highlighting against escaping, minimum query length, facet drilldown, label resolution and the cap notice), the publication axis in the same file, where a page of the real holding with two years and two languages is filtered and counted and each facet count is checked against the list its value opens, and the history guarantee and error routes in `tests/routing_guard.test.js`. `node --check` validates every module syntactically. The Python suite checks the generated data contracts (including the frontend projection contract) and calls the Node tests through the shared test bridge.

A local smoke test runs with a static server over `docs/`:

```bash
python -m http.server 8000 --directory docs
```

The application is then reachable at `http://localhost:8000/`; the curation surface is active only there.

## Deployment and Limits

GitHub Pages publishes the content of `docs/`. The application performs no live write-back. Exports are downloaded locally and then integrated as reviewed repository patches.

The reading view separates the publications of a page wherever the record carries the layer; a page without it keeps the flat projection and says so. The edit layout still adjudicates the four flat fields only, so a correction is scoped to the page rather than to one of its publications, which the card now says at the page record and at every editable row. Curating a publication field needs a patch target per publication identifier, which the patch contract does not carry, and editing per publication stays outside version 1.0 (decided by the main instance after delegation by the operator on 2026-09-23, revisable). The canonical RDF does not carry the publication layer, and the edition graph stays the reference structure for multi-edition pages. The per-entry JSON-LD download carries the review state, the claim references and, for a restored page, `klawiter:sourceRevision`. Field provenance, review metadata and released frontend corrections are not uniformly present in canonical/export/playground representations. The edition graph and its queue are the reference for those cases. Title editing and institutional work decisions are outside the current local editor.
