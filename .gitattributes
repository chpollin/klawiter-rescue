# Normalize all text to LF so read_bytes()-based provenance hashes
# (segment_editions._code_hash, gate manifests) are platform-independent.
* text=auto eol=lf
data/raw/** binary

# The archival dump holds account and edit metadata; it stays out of release archives.
data/raw export-ignore
